%h3 
  =active_category
%hr
%br

%table
  - if @posts.present?
    - @posts.each do |post|
      %tr
        %td{:style=>'width:300px'}= post.title
        %td= link_to 'Show', post
  -else
    %h4
      -if params[:category_id]
        There are no listings for this category right now.
      -else
        Please select your location above and then select the category on the right to get started.
%br
